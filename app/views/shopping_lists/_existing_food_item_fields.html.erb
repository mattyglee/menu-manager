<div class="control-group fields">
	<div class="controls">
	  <%= f.text_field :quantity, :class => "input-mini" %>
	  <%= Food.find(f.object.food_id).shopping_list_name(f.object.quantity) %>
		<%= f.hidden_field :food_id %>
	  <%= link_to_remove_fields "", f %>
	</div>
</div>
